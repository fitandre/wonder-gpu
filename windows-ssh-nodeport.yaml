apiVersion: v1
kind: Service
metadata:
  name: windows-ssh-nodeport
spec:
  type: NodePort
  selector:
    kubevirt.io/vm: windows-2025-test
  ports:
  - port: 22
    targetPort: 22
    nodePort: 30022
